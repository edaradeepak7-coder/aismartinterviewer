'use client';
import AppLayout from '@/components/AppLayout';
import PreLaunchContent from './components/PreLaunchContent';

export default function PreLaunchPage() {
  return (
    <AppLayout>
      <PreLaunchContent />
    </AppLayout>
  );
}
