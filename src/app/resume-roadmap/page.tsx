'use client';
import AppLayout from '@/components/AppLayout';
import ResumeRoadmapContent from './components/ResumeRoadmapContent';

export default function ResumeRoadmapPage() {
  return (
    <AppLayout role="candidate">
      <ResumeRoadmapContent />
    </AppLayout>
  );
}
