import InstitutionSetupWizard from './components/InstitutionSetupWizard';

export const metadata = {
  title: 'Institution Setup | Triveda AI Platform',
  description: 'Set up your institution workspace on Triveda AI Platform',
};

export default function InstitutionSetupPage() {
  return <InstitutionSetupWizard />;
}
